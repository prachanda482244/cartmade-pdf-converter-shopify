const LivePreview = () => {
  return (
    <div
      style={{
        marginBottom: "15px",
        fontSize: "18px",
        fontWeight: "bold",
      }}
    >
      Live preview:
    </div>
  );
};

export default LivePreview;
